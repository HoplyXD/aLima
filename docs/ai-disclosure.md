# aLima AI Usage Disclosure

This is the running disclosure log for AI used during development or inside the game. Append entries when a new AI tool, model, generated asset, runtime integration, or materially AI-assisted workflow is introduced. Do not remove old entries; add corrections as new dated rows.

## Disclosure Rules

- Record the tool/model, date, area, concrete use, human review, and shipped output.
- Distinguish development assistance from runtime AI visible to players.
- Record generated art, music, voices, dialogue, lore, code, and research separately.
- Historical claims require verification against credible sources; AI output is never a source of fact.
- Never use or imitate copyrighted recordings or undisclosed third-party assets.
- Runtime keys and prompts stay in the backend; no secret is committed to the Godot client.

## Usage Log

| Date | Tool / model | Area | Use | Human review and safeguards | Shipped output |
|---|---|---|---|---|---|
| 2026-06-15 | OpenAI Codex | Documentation and repository audit | Reconciled `CLAUDE.md`, `README.md`, and `docs/PRD.md`; created the canonical implementation checklist; inspected the Godot project and Git state. | Team must review all requirements and cultural wording. No historical claim, art, audio, dialogue, or runtime response was generated. Existing invariants and requirement IDs were preserved. | Documentation only |
| 2026-06-15 | OpenAI Codex | Full-GDD documentation reconciliation | Reconciled the README GDD, PRD, implementation checklist, operating contract, and prompt context so that checklist completion requires the complete promised game; added measurable content, production, platform/input, live-service, playtest, and release gates while preserving completed Phase 0–1 history. | Team must review the new scope, minimum content counts, phase sequencing, performance targets, and cultural-review responsibilities before treating them as committed production estimates. This change is documentation only: it generated no historical claims, dialogue, art, audio, voice, or other production asset. | `README.md`, `CLAUDE.md`, `docs/PRD.md`, `docs/phase-task.md`, `docs/PROMPT_CONTEXT.md`, and this disclosure |
| 2026-06-15 | Kimi Code CLI (Moonshot AI) | Phase 0 clock stabilization and documentation | AI-assisted implementation: added minute-level placeholder clock progression (`7:00 AM` → `7:01 AM`) derived from configurable `seconds_per_hour`; preserved dialogue pause/resume and day/loop wrapping; added focused GUT tests (`tests/test_shop_clock.gd`); updated `docs/phase-task.md`, `docs/PROMPT_CONTEXT.md`, and `CLAUDE.md` with verified command results and the bare-`godot` 4.5.1 blocker. | Team must review the clock math, test coverage, and documentation accuracy. No historical claim, art, audio, dialogue, or runtime AI was generated. | `scripts/shop/shop_controller.gd`, `scenes/ui/shop_hud.gd`, `tests/test_shop_clock.gd` + `.uid`, documentation updates |
| 2026-06-15 | Claude Code (Claude Opus 4.8) | Phase 0 repository and Shop stabilization | AI-assisted refactor: consolidated three duplicate Shop controllers into a typed controller (`scripts/shop/shop_controller.gd`) plus a presentation-only HUD (`scenes/ui/shop_hud.gd`); stabilized `scenes/Shop.tscn` (revealed HUD, removed the stray test button); vendored GUT 9.6.0 with a headless smoke test; added pinned gdtoolkit lint/format config and a GitHub Actions CI workflow; set up the Godot 4.6.3 PATH selection and `.gitignore` hygiene. | Team must review the refactor and tooling. No historical claim, art, audio, dialogue, or runtime AI was generated; existing placeholder Auntie/"coming soon" prose was migrated verbatim from prior code. All checks pass under Godot 4.6.3 (clean import, GUT 7/7, gdformat, gdlint). | GDScript controller/HUD split, GUT smoke test, CI workflow, lint/format + GUT config |
| 2026-06-15 | Kimi Code CLI (Moonshot AI) | Phase 1 core architecture and data | AI-assisted implementation: created typed models (`scripts/models/`), shared enums/validation helpers, `DataRepository` (`scripts/core/data_repository.gd`), core autoloads (`EventBus`, `GameState`, `SaveService`), deterministic run context, and Phase 1 slice JSON fixtures under `data/`; added GUT coverage in `tests/models/` and `tests/core/`; updated `project.godot`, `docs/phase-task.md`, `docs/PROMPT_CONTEXT.md`, `CLAUDE.md`, and this disclosure. | Team must review all schemas, fixture data, and tests. No historical claim, art, audio, dialogue, or runtime AI was generated. Fixtures use explicit placeholder audio-path strategy and artifact-agnostic data. All checks pass under Godot 4.6.3 (import, GUT 49/49, gdformat, gdlint). | Typed models, repository, autoloads, data fixtures, tests, documentation |
| 2026-06-15 | Claude Code (Claude Opus 4.8) | Phase 2 clock, loop, and persistence | AI-assisted implementation: added the `DayClock` autoload (typed, presentation-free, tick-driven; 07:00–20:00 close, debug speed, ref-counted pause ownership) and the `LoopController` autoload (Day 1–5 progression, deterministic five-day split reset, EventBus/GameState bridge); integrated the Shop controller with the clock + pause API; added strict load validation and injectable save paths to `SaveService`; wrote `tests/core/test_day_clock.gd` and `test_loop_controller.gd`, expanded `test_save_service.gd`, and migrated `tests/test_shop_clock.gd`; updated `docs/phase-task.md`, `docs/PROMPT_CONTEXT.md`, `CLAUDE.md`, and this disclosure. | Team must review the clock/loop math, the persistence-split reset order, and the test coverage. No historical claim, art, audio, dialogue, or runtime AI was generated. Automated checks pass under Godot 4.6.3 (import; GUT 69/69, 234 asserts; gdformat; gdlint; `git diff --check`). On-screen/real-time clock observation is left as a pending manual gate. | DayClock + LoopController autoloads, SaveService hardening, Shop integration, GUT tests, documentation updates |
| 2026-06-15 | Claude Code (Claude Opus 4.8) | Phase 3 delivery and triage | AI-assisted implementation: added `DeliveryGenerator`, `SpawnDirector` (minimal Phase 3), `GlowMapper`, `TriageState`, `TriageService`, `DeliveryConfig`, and the `TriageScreen` UI; added `Marker3D` anchors to `scenes/Shop.tscn`; extended `DataRepository`, `SaveState`, `GameState`, `LoopController`, `ShopController`, `ShopHud`, and object models; authored `data/delivery/delivery_config.json`; added 32 GUT tests under `tests/delivery/`; updated `docs/phase-task.md` and this disclosure. | Team must review the delivery/triage math, storage-cost semantics, carrier identity rules, and UI navigation. No historical claim, art, audio, dialogue, or runtime AI was generated. Automated checks pass under Godot 4.6.3 (import; focused delivery suite 32/32, 191 asserts; complete GUT suite 101/101, 429 asserts; gdformat; gdlint; `git diff --check`). On-screen mouse/keyboard readability and three accelerated debug mornings remain a pending human verification gate. | Delivery/triage generator/director/service/UI, placement anchors, glow mapper, data/config, tests, documentation |
| 2026-06-15 | Kimi Code CLI (Moonshot AI) | Phase 3/4 bug fix: repeated morning delivery UID collision + daily gate | AI-assisted fix: `DeliveryGenerator._make_uid` now checks existing loop inventory and `current_delivery_ids` before returning a candidate, so a second Morning Delivery on the same day/loop no longer reuses UIDs from the first batch. Added `LoopState.last_delivery_day` and gated `ShopController._on_morning_delivery_pressed` to one delivery per in-game day. Added `test_unique_instance_ids_across_repeated_morning_deliveries` to `tests/delivery/test_delivery_generator.gd`. | Team must review the UID uniqueness logic, daily-gate edge cases (save/load, day/loop transitions), and the small catalog repetition (only two templates in the current slice). No historical claim, art, audio, dialogue, or runtime AI was generated. Automated checks pass under Godot 4.6.3 (import; focused delivery suite 33/33, 197 asserts; complete GUT suite 121/121, 509 asserts; gdformat; gdlint; `git diff --check`). | `scripts/delivery/delivery_generator.gd`, `scripts/shop/shop_controller.gd`, `scripts/models/save_state.gd`, `tests/delivery/test_delivery_generator.gd`, documentation updates |
| 2026-06-15 | Claude Code (Claude Opus 4.8) | Phase 4 restoration and pendant carrier | AI-assisted implementation: added `RestorationService` and `RestorationScreen` (`scenes/ui/restoration_screen.gd`/`.tscn`); extended `ObjectInstance`, `ScrapObjectTemplate`, `TechniqueDefinition`, `DeliveryGenerator`, and `LoopController` (starting-kit grant); wired the workbench button to the restoration UI in `scripts/shop/shop_controller.gd`; authored pendant/tin cleaning tuning in `data/objects/objects.json` and technique bonuses in `data/objects/techniques.json`; added 19 GUT tests under `tests/restoration/`. | Team must review the restoration tuning values, UI readability at 1920x1080, and the manual clean/open flow. No historical claim, art, audio, dialogue, or runtime AI was generated. Automated checks pass under Godot 4.6.3 (import; focused restoration suite 19/19, 73 asserts; complete GUT suite 120/120, 503 asserts; gdformat; gdlint; `git diff --check`). On-screen manual verification of the ordinary and promoted pendant flows remains pending. | Restoration service/UI, tool-consequence rules, clean/open state machine, data tuning, tests, documentation |
| 2026-06-15 | Kimi Code CLI (Moonshot AI) | Phase 5 Spawn Director | AI-assisted implementation: rewrote `scripts/delivery/spawn_director.gd` with explicit candidate enumeration, hard filters, weighted scoring, never-twice history, documented soft reset, and deterministic audit log; added typed `scripts/discovery/placement_candidate.gd`; added `data/delivery/spawn_config.json` tuning; added `scripts/discovery/spawn_director_demo.gd` for three-seed demo runs; updated `DeliveryGenerator` to emit `EventBus.carrier_activated` on promotion; expanded fixtures to give the Director enough valid carriers; added `tests/discovery/spawn_director/test_spawn_director_phase5_filters.gd` (16 tests) and `tests/discovery/spawn_director/test_spawn_director_phase5_history.gd` (9 tests). Fixed cross-test singleton pollution by reloading `DataRepository` from disk in the Phase 5 suite setup, and updated `tests/core/test_loop_controller.gd` to assert that a manually injected placement placeholder is replaced by a real SpawnDirector plan after loop reset. | Team must review the spawn weights, never-twice/soft-reset semantics, and the small fixture catalog. No historical claim, art, audio, dialogue, or runtime AI was generated. Automated checks pass under Godot 4.6.3 (import; focused Phase 5 suite 25/25; complete GUT suite 146/146, 565 asserts; gdformat; gdlint; `git diff --check`). | `scripts/delivery/spawn_director.gd`, `scripts/discovery/placement_candidate.gd`, `scripts/discovery/spawn_director_demo.gd`, `scripts/delivery/delivery_generator.gd`, `data/delivery/spawn_config.json`, `tests/discovery/spawn_director/test_spawn_director_phase5_filters.gd`, `tests/discovery/spawn_director/test_spawn_director_phase5_history.gd`, `tests/core/test_loop_controller.gd`, documentation updates |
| 2026-06-15 | Claude Code (Claude Opus 4.8) | Presentation-contract docs change: 3D restoration + hybrid 2D/3D journal | AI-assisted **documentation-only** change reflecting a design decision: restoration becomes a focused **3D** object-manipulation interaction (rotate + clean the 3D object's surface; new requirement **REST-R8**, open decisions **D8/D9**) and the journal becomes **hybrid 2D/3D** (new requirement **JRN-R6**); extended INPUT-R3/R5 and PLAT-R4 for the 3D interaction. Updated `CLAUDE.md`, `README.md`, `docs/PRD.md`, `docs/phase-task.md` (reopened P4.1/P4.4 to `[-]`, added task **P4.7** for the 3D view, annotated Phase 9/13), `docs/PROMPT_CONTEXT.md`, and this disclosure. No game code changed: `RestorationService` logic and tests are unaffected; the implemented `scenes/ui/restoration_screen.*` is recorded as a 2D interim placeholder pending the P4.7 3D rework. | Team must review the 3D restoration / hybrid-journal presentation contract and confirm the deferred code rework (P4.7 / Phase 9). No code, historical claim, art, audio, dialogue, or runtime AI was generated. Verified docs-only: `git diff --check` clean; consistency grep confirms no doc still calls restoration/journal a purely 2D screen; GUT/gdformat/gdlint unaffected (no code touched). | `CLAUDE.md`, `README.md`, `docs/PRD.md`, `docs/phase-task.md`, `docs/PROMPT_CONTEXT.md`, and this disclosure |
| 2026-06-15 | OpenAI Codex | Prompt engineering and repository audit | Inspected all tracked source, scenes, assets, narrative prototypes, project settings, documentation, Git history, and local verification tools; created `docs/PROMPT_CONTEXT.md` as canonical context for future implementation prompts. | Findings distinguish verified runtime behavior from plans and assumptions. No game feature, historical claim, art, audio, dialogue, or runtime AI output was generated. Team review remains required before using prompts or submission claims. | Documentation only |
| 2026-06-16 | Claude Code (Claude Opus 4.8) | Phase 4 P4.7 focused 3D restoration view (REST-R8) | AI-assisted implementation: built the focused **3D** restoration view that replaces the retired 2D placeholder — `scripts/restoration/restoration_view.gd` (`RestorationView`: SubViewport 3D + 2D HUD, input/pause/service delegation), `scripts/restoration/restoration_object_3d.gd` (`RestorationObject3D`: placeholder pendant geometry, surface dirt mask, analytic ray/UV hit-testing, clasp), `scenes/restoration/restoration_view.tscn`, and `scenes/restoration/restoration_dirt.gdshader` (GL-Compatibility/HTML5 dirt-mask shader). Rewired the Workbench in `scripts/shop/shop_controller.gd`; retired `scenes/ui/restoration_screen.*` and migrated its GUT coverage into `tests/restoration/test_restoration_view.gd` (15 tests). `RestorationService` and its 17 logic tests are unchanged: the view delegates all rules to it and never branches on carrier identity. Updated `docs/phase-task.md`, `docs/PROMPT_CONTEXT.md`, `CLAUDE.md`, and this log. | Team must review the 3D presentation, the placeholder development geometry/shader (to be replaced by authored models in Phase 13/20), the input scheme, and the surface-stroke→service threshold mapping. No historical claim, art, audio, dialogue, or runtime AI was generated; the shader and meshes are original placeholder dev geometry. Automated checks pass under Godot 4.6.3 (import; restoration suite 32/32, 121 asserts; complete GUT suite 159/159, 613 asserts; gdformat; gdlint; `git diff --check` clean). On-screen mouse verification and controller/touch device verification remain pending human gates. | `scripts/restoration/restoration_view.gd`, `scripts/restoration/restoration_object_3d.gd`, `scenes/restoration/restoration_view.tscn`, `scenes/restoration/restoration_dirt.gdshader`, `scripts/shop/shop_controller.gd`, `tests/restoration/test_restoration_view.gd`, retired `scenes/ui/restoration_screen.*` + `tests/restoration/test_restoration_ui.gd`, documentation updates |
| 2026-06-16 | Kimi Code CLI (Moonshot AI) | Phase 6 Cultural Echoes | AI-assisted implementation: added the `default_bus_layout.tres` (Master/Hum/Melody/Voice/Heartbeat); extended `EchoSet` with proximity tuning and band thresholds; generated original placeholder WAV audio via `scripts/audio/generate_echo_placeholders.py` into `assets/audio/echoes/`; implemented `EchoAudio`, `EchoMixer`, `EchoProximityService`, `EchoController`, `EchoHud`, `EchoFocusController`, and `EchoSceneBridge`; wired them into `scenes/Shop.tscn` and `scenes/ui/echo_hud.tscn`; fixed the `AudioStreamWAV.loop` incompatibility by setting `loop` for Ogg/MP3 and `loop_mode = LOOP_FORWARD` for WAV; added 48 GUT tests under `tests/discovery/echoes/`. | Team must review the placeholder audio (to be replaced with culturally reviewed original recordings), the authored band thresholds, and the focus/source-position seam. No historical claim, dialogue, or runtime AI output was generated. Automated checks pass under Godot 4.6.3 (import; focused Echo suite 48/48; complete GUT suite 207/207, 710 asserts; gdformat; gdlint; `git diff --check` clean). Manual on-screen verification of audio mixing, meter/caption readability, and carrier flicker reveal remains pending. | `default_bus_layout.tres`, `scripts/discovery/echo_*.gd`, `scripts/shop/echo_*.gd`, `scenes/ui/echo_hud.*`, `scripts/models/echo_set.gd`, `data/echoes/echoes.json`, `assets/audio/echoes/*.wav`, `tests/discovery/echoes/*.gd`, `scripts/audio/generate_echo_placeholders.py`, documentation updates |
| 2026-06-16 | Kimi Code CLI (Moonshot AI) | Phase 7 cached scanner | AI-assisted completion/verification: inspected existing `ScannerService`, `ScannerScreen`, `ScannerCacheTransport`, `ScannerRequest`/`ScannerResponse` models, and `data/scanner-cache/scanner_cache.json`; verified they satisfy P7.1–P7.5 and PRD SCAN-R1/R2/R4/R5. Fixed unrelated full-suite blockers (`scenes/Book/Book.gd` invalid variable name `animationPlayer` → `animation_player`; `scripts/restoration/restoration_view.gd` class-definitions-order lint error by moving `_journal_viewport` to the member-variable block). Added/updated documentation in `docs/phase-task.md` (P7 tasks, verification commands, manual-pending notes, cache source-gap disclosure), `docs/PROMPT_CONTEXT.md` (scanner implemented, test counts, missing-system list), and this disclosure. | Team must review scanner cache wording, source gaps, and UI readability. All cached cultural/historical claims are marked `unverified` and noted as development placeholders pending workshop review; no fabricated citations were added. No API keys, direct provider calls, or runtime AI output were introduced. Automated checks pass under Godot 4.6.3 (import; focused scanner suite 28/28, 105 asserts; complete GUT suite 235/235, 815 asserts; gdformat; gdlint; `git diff --check` clean; tracked-secret and provider-string scans returned no matches). Manual on-screen verification of normal/suspicious fixture scanning, 1920x1080/1280x720 readability, and pause-ownership composition remains pending. | Existing `scripts/scanner/*`, `scripts/models/scanner_*.gd`, `scenes/ui/scanner_screen.tscn`, `data/scanner-cache/scanner_cache.json`, `tests/scanner/*.gd`, plus `scenes/Book/Book.gd` and `scripts/restoration/restoration_view.gd` lint fixes, documentation updates |
| 2026-06-16 | Kimi Code CLI (Moonshot AI) | Phase 8 backend, mock Portal, and Found/Unlock flow | AI-assisted implementation: scaffolded `server/` (Express, env validation, `.env.example`, rate limiting, validators, thin controllers, Jest tests) with cached `POST /api/scan` and `POST /api/portal/discovery` proxy including `player_id:fragment_id` idempotency, timeout, and deterministic fallback; scaffolded `mock-portal/` with `POST /discovery` and deterministic fact cards; added Godot `PortalClient`, `PortalDiscoveryRequest`/`PortalDiscoveryResponse`/`PortalResult` models, `PortalFlowController`, `ArtifactFoundScreen`, `PortalUnlockScreen`, and `SeatingService`; wired `EventBus.portal_completed` to include `fact_card` and `EventBus.fragment_seated`; added `network/portal/backend_url` project setting and `DayClock.PAUSE_PORTAL` usage; added `ModelUtils.as_dictionary`; added `tests/portal/` (15 GUT tests); updated `.gitignore` for `node_modules/` and `server/cache/`; updated `docs/phase-task.md`, `docs/PROMPT_CONTEXT.md`, and this disclosure. | Team must review backend env vars, fallback fact-card wording, Godot UI scaling, and the manual end-to-end flow. Fact cards are development placeholders pending workshop/cultural review. No API keys, direct provider URLs, or runtime AI calls exist in Godot; all Portal traffic goes through the backend. Automated checks pass under Godot 4.6.3 (import; focused portal suite 15/15, 45 asserts; complete GUT suite 250/250, 860 asserts; gdformat; gdlint; `git diff --check` clean; tracked-secret and provider-string scans returned no matches). Backend suites pass: `server/npm test` 12/12, `mock-portal/npm test` 4/4. Manual on-screen verification of success, timeout/fallback, and duplicate-click paths remains pending. | `server/`, `mock-portal/`, `scripts/portal/*`, `scripts/journal/seating_service.gd`, `scripts/shop/portal_flow_controller.gd`, `scripts/models/model_utils.gd`, `scripts/core/event_bus.gd`, `scenes/ui/artifact_found_screen.tscn`, `scenes/ui/portal_unlock_screen.tscn`, `project.godot`, `.gitignore`, `tests/portal/*`, documentation updates |
| 2026-06-17 | Claude Code (Claude Opus 4.8) | Auntie quest line: decal-based restoration foundation + data (Phase 10/13 down-payment) | AI-assisted implementation: added a data-driven decal model (`scripts/models/surface_decal.gd`, `SurfaceDecal` with placeholder hex `color` swappable for a future texture, `required_tool`, `to_color()`); extended `ScrapObjectTemplate` (authored `decals`, `requires_join`, `join_tool`, and a `deliverable` flag) and `ObjectInstance` (runtime `removed_decals`, `is_joined`); added `RestorationService.clean_decal()` (matching tool clears a decal and raises condition/value; wrong tool applies the existing wrong-tool damage; clearing the last decal reaches CLEAN and emits `restoration_completed`) and `join_object()` (clean-gated, archival-tape-gated, idempotent reassembly of torn halves); excluded non-`deliverable` quest items from the delivery pool; extended `CharacterRoute` with authored `beats` and added cross-reference validation for decal tools, join tools, and beat templates. Authored Auntie's 3 quests as data: new tools (`soft_brush`, `damp_cloth`, `photo_kit`, `stain_lifter`, `polishing_cloth`, `consolidant`, `solvent`, `archival_tape`) in `data/objects/tools.json`; three non-deliverable quest templates (`auntie_photo_faded`, `auntie_frame_portrait`, `auntie_halfphoto_torn`) with decal sets in `data/objects/objects.json`; and Auntie's Day 1/3/5 beats in `data/routes/routes.json`. Added `tests/restoration/test_decal_restoration.gd` (11 tests). Decal colours are placeholders pending authored textures; the photo/frame presentation will reuse a scaled-down Book `Page` (PNG-slottable) and a future 3D frame model. NPC visit scheduling/Day-5 gating is NOT implemented here (deferred to Phase 10 P10.2 / Phase 15). | Team must review the decal/tool tuning, the rarity/value ranges of the quest items, the join mechanic, and the placeholder colours (to be replaced with reviewed textures). No historical claim, art, audio, dialogue, or runtime AI was generated; the Auntie beat summaries are development placeholder prose pending cultural/native-speaker review. Automated checks pass under Godot 4.6.stable on this machine (the documented 4.6.3 console build is not installed here): import; restoration suite 55/55, 203 asserts; complete GUT suite 292/292, 997 asserts; `git diff --check` clean. gdtoolkit (gdformat/gdlint) is not installed locally and was not run; CI remains the lint/format gate and the new code follows the repository's tab/blank-line conventions. | `scripts/models/surface_decal.gd`, `scripts/models/scrap_object_template.gd`, `scripts/models/object_instance.gd`, `scripts/models/character_route.gd`, `scripts/restoration/restoration_service.gd`, `scripts/delivery/delivery_generator.gd`, `scripts/core/data_repository.gd`, `data/objects/tools.json`, `data/objects/objects.json`, `data/routes/routes.json`, `tests/restoration/test_decal_restoration.gd`, documentation updates |
| 2026-06-17 | Claude Code (Claude Opus 4.8) | Diegetic 3D presentation: visible tool props (P4.8 / REST-R9) + shop interactables (P4.9 / SHELL-R1/R2) | AI-assisted design + implementation making interactions physically 3D. Added `scripts/restoration/restoration_tool_tray.gd` (`RestorationToolTray`: presentation-only, data-driven, selectable 3D cleaning-tool props with analytic ray pick + highlight/pose) and a `ToolTray` node in `restoration_view.tscn`; integrated prop picking + a `restoration_cycle_tool` action into `restoration_view.gd` and demoted the HUD tool buttons to a labelled accessibility/fallback. Added the reusable `scripts/shop/interactable_3d.gd` (`Interactable3D`: hover prompt/highlight + `activated` signal, no game logic), five `Interactables/*` placeholder props + HUD `PromptLabel`/`FallbackHint` in `scenes/Shop.tscn`, and wired them into `scripts/shop/shop_controller.gd` (viewport picking, hover→prompt, overlay gating) reusing the existing handlers. Added GUT tests (`tests/restoration/test_restoration_tool_tray.gd`, `test_restoration_tool_props_view.gd`, `tests/shop/test_interactable_3d.gd`, extended `tests/test_shop_smoke.gd`). Refined `docs/PRD.md` (REST-R8 + new REST-R9, new SHELL-R1/R2), `docs/phase-task.md` (P4.8/P4.9), `docs/PROMPT_CONTEXT.md`, `README.md`, `CLAUDE.md`, and this log. | Team must review the placeholder development geometry (cloth/brush props and the shop door/bench/journal/phone/delivery boxes — **not** final art; replaced in Phase 13/20), the prop layout/framing, and the per-overlay input-blocking behavior. `RestorationService` and its 17 logic tests are unchanged; the props/interactables are presentation-only and never read carrier identity. No historical claim, art, audio, dialogue, or runtime AI was generated; all meshes/materials are original placeholder dev geometry. Automated checks pass under Godot 4.6.3 (import; restoration suite 44/44, 152 asserts; shop suite 6/6; complete GUT suite 273/273, 921 asserts; gdformat; gdlint; `git diff --check` clean). On-screen mouse/controller/touch verification of tool-prop selection and shop click-through remains a pending human gate. | `scripts/restoration/restoration_tool_tray.gd`, `scripts/restoration/restoration_view.gd`, `scenes/restoration/restoration_view.tscn`, `scripts/shop/interactable_3d.gd`, `scripts/shop/shop_controller.gd`, `scenes/Shop.tscn`, `scenes/ui/shop_hud.gd`, `tests/restoration/test_restoration_tool_tray.gd`, `tests/restoration/test_restoration_tool_props_view.gd`, `tests/restoration/test_restoration_view.gd`, `tests/shop/test_interactable_3d.gd`, `tests/test_shop_smoke.gd`, documentation updates |
| 2026-06-17 | Kimi Code CLI (Moonshot AI) | Phase 9 journal and fragment case | AI-assisted implementation: added `JournalService` autoload (`scripts/journal/journal_service.gd`) that creates/updates one stable `JournalEntry` per Purple-and-below template on `EventBus.restoration_completed` and updates it with `player_verdict` + scanner annotations on `EventBus.scanner_verdict_committed`; extended `JournalEntry` with `player_verdict`; reused the existing `BookViewport`/`JournalBook`/`Page` SubViewport book and added `DayClock.PAUSE_JOURNAL` pause ownership; rendered the five-slot Fragment Case on page 4 using `case_slot_index` from `data/artifacts/master_artifact.json` with placeholder 2D slot art, an object-archive index on page 5, and individual entry pages from page 6; wired `JournalBook` to refresh on `EventBus.fragment_seated`; added `DayClock.has_pause_owner()` to avoid double-release warnings; added 21 GUT tests under `tests/journal/`; updated `docs/phase-task.md`, `docs/PROMPT_CONTEXT.md`, `CLAUDE.md`, and this disclosure. | Team must review the placeholder 2D slot art (clearly not final; rotatable 3D fragment viewers and per-entry 3D object previews are Phase 16 polish), the journal page layout at 1920x1080, and the rarity-routing boundary. No historical claim, art, audio, dialogue, or runtime AI output was generated; placeholder slot panels are original development geometry/colors. Automated checks pass under Godot 4.6.3 (import; focused journal suite 21/21, 43 asserts; portal suite 15/15, 45 asserts; complete GUT suite 294/294, 964 asserts; gdformat; gdlint; `git diff --check` clean). Manual on-screen verification of the journal open/close pause flow, the five-slot case, and the Found→Portal→slot-fill sequence remains pending. | `scripts/journal/journal_service.gd`, `scripts/models/journal_entry.gd`, `scenes/Book/Page.gd`, `scenes/Book/Book.gd`, `scenes/Book/book_viewport.gd`, `scripts/core/day_clock.gd`, `project.godot`, `tests/journal/*.gd`, documentation updates |
| 2026-06-17 | Claude Code (Claude Opus 4.8) | Phase 9 journal entry updates and rarity routing (P9.2/P9.3, partial) | AI-assisted implementation: added the `JournalService` autoload (`scripts/journal/journal_service.gd`) that listens to `EventBus.restoration_completed` and `EventBus.scanner_verdict_committed`, creates one stable persistent `JournalEntry` per template on first restoration and updates it in place (no duplicates, best-condition kept as the maximum), writes scanner snapshot data to `ai_annotations`/`counterfeit_indicators` separate from `uncle_notes`, and routes by rarity via `is_journal_rarity()` so Gold-and-above are excluded from the journal (they route to the museum via `SeatingService`). Registered the autoload in `project.godot`; added `tests/journal/test_journal_service.gd` (8 tests); updated `docs/phase-task.md` and this disclosure. The hybrid 2D/3D journal UI (P9.1) and five-slot case visual (P9.4) are not implemented. | Team must review the rarity-routing boundary, entry seeding/update fields, and the deferred sale-data (`best_sale`, Phase 14) wiring. No historical claim, art, audio, dialogue, or runtime AI was generated. Automated checks pass under Godot 4.6.stable on this machine (the documented 4.6.3 console build is not installed here): import; focused journal suite 8/8, 25 asserts; complete GUT suite 258/258, 885 asserts. gdtoolkit (gdformat/gdlint) is not installed on this machine and was not run locally; CI remains responsible for the lint/format gate, and the new files follow the repository's tab/blank-line conventions. | `scripts/journal/journal_service.gd`, `project.godot`, `tests/journal/test_journal_service.gd`, documentation updates |
| 2026-06-18 | Claude Code (Claude Opus 4.8) | Authored Storage scene + fix: preserve exact cleaned spots when switching artifacts | AI-assisted, two changes. (1) **Storage as an authored scene:** rewrote `scenes/ui/storage_screen.tscn` from a script-only CanvasLayer into a real node tree (dim backdrop, margin, title, a `TabContainer` with `Artifacts` / `Tools` / `Key Items` tabs each holding a `ScrollContainer`→`VBoxContainer`, a status label, and a Close button), all via `unique_name_in_owner`; `storage_screen.gd` now binds those nodes with `@onready` and only populates the dynamic rows (its `_build_ui`/`_make_tab` code-build path was removed). Behaviour and the 6 storage tests are unchanged. (2) **Restoration switch bug fix:** switching artifacts mid-clean and back rebuilt the pendant's grime purely from the saved `condition` number (a deterministic noise scatter), so the player's actual cleaned spots visibly changed. Added a per-instance dirt-mask cache in `RestorationView` (`_dirt_cache`, snapshotted before switching and on close, restored on load) backed by `RestorationObject3D.snapshot_dirt()` / `restore_dirt()` / `is_uv_cleaned()`. Decal photos already persisted their `removed_decals`, so only condition-based masks are cached; the cache is in-memory (a fresh save/reload still reconstructs from condition). Added `test_switching_artifacts_preserves_exact_cleaned_spots` to `tests/restoration/test_restoration_view.gd`. | Team must review the authored Storage layout on-screen and confirm the cleaned-spot preservation feels right when switching back and forth (and that decal photos still restore from `removed_decals`). No historical claim, art, audio, dialogue, or runtime AI was generated. Automated checks pass under Godot 4.6.stable (the documented 4.6.3 console build is not installed here): import; economy suite 25/25; restoration suite 63/63; complete GUT suite 347/347; `git diff --check` clean. gdtoolkit not installed locally; CI remains the lint/format gate. On-screen verification is a pending manual gate. | `scenes/ui/storage_screen.tscn`, `scripts/economy/storage_screen.gd`, `scripts/restoration/restoration_view.gd`, `scripts/restoration/restoration_object_3d.gd`, `tests/restoration/test_restoration_view.gd`, documentation updates |
| 2026-06-18 | Claude Code (Claude Opus 4.8) | Authored phone scene (home screen + Marketplace app) replacing the code-built screen | AI-assisted: turned the phone into its own authored scene with real nodes (per the team's request to make it "like Book.tscn — its own tscn and nodes"). New `scenes/ui/phone.tscn` is a phone-frame layout (dim backdrop, centered rounded phone Body via StyleBoxFlat, top bar with title + close, a Screen panel, bottom bar with a Home button + feedback) with `unique_name_in_owner` nodes the script drives. `scripts/economy/phone.gd` (`class_name Phone`) handles home↔app navigation (home shows an app grid with a Marketplace icon + a disabled "Soon" slot; tapping opens the Marketplace app inside the screen), renders the buy list, delegates purchases to `MarketplaceService`, owns `DayClock.PAUSE_PHONE`, and uses Esc as back/close. The old code-built `MarketplaceScreen` (`.gd`/`.tscn`) and `test_marketplace_screen.gd` were removed; `ShopController` and the restoration bench now open the `Phone` (the shop HUD Phone button and the bench Phone button both reach the same instance). Updated `tests/test_shop_smoke.gd` and `tests/restoration/test_bench_overlays.gd`; added `tests/economy/test_phone.gd` (4 tests: home/app nav, pause, buy-in-app). | Team must review the phone-frame layout/readability on-screen (1920x1080 / 1280x720), the app-grid affordance, and back/close behaviour with mouse + controller/touch. No historical claim, art, audio, dialogue, or runtime AI was generated; the phone body/screen are placeholder styled panels (no authored art). Automated checks pass under Godot 4.6.stable (the documented 4.6.3 console build is not installed here): import (phone.tscn loads, Shop opens); complete GUT suite 346/346; `git diff --check` clean. gdtoolkit not installed locally; CI remains the lint/format gate. On-screen verification of the phone is a pending manual gate. | `scenes/ui/phone.tscn`, `scripts/economy/phone.gd`, `scripts/shop/shop_controller.gd`, `scripts/restoration/restoration_view.gd`, removed `scripts/economy/marketplace_screen.gd`+`scenes/ui/marketplace_screen.tscn`+`tests/economy/test_marketplace_screen.gd`, `tests/test_shop_smoke.gd`, `tests/restoration/test_bench_overlays.gd`, `tests/economy/test_phone.gd`, documentation updates |
| 2026-06-18 | Claude Code (Claude Opus 4.8) | Bench access to Storage/Marketplace overlays + Book.tscn→Journal.tscn rename | AI-assisted: the focused 3D restoration bench can now open the shared **Storage** and **Marketplace** overlays (the same screens the Shop HUD buttons open), matching how it already opens the Journal. Added a **Storage** button to the bench HUD button row (`scenes/restoration/restoration_view.tscn`), a `storage_requested` signal, and `set_storage_screen()`/`set_marketplace_screen()` setters; the bench's Phone button now opens the real Marketplace (was a placeholder). `ShopController` hands its `_marketplace_screen`/`_storage_screen` to the bench. Renamed `scenes/Book/Book.tscn` → `scenes/Book/Journal.tscn` (via `git mv`, uid preserved) and updated the `BookViewport.tscn` reference. Added `tests/restoration/test_bench_overlays.gd` (2 tests). | Team must review on-screen that the bench Journal/Phone/Storage buttons open the right overlays and layer/return correctly, and that the journal still loads after the rename. No historical claim, art, audio, dialogue, or runtime AI was generated. Automated checks pass under Godot 4.6.stable (the documented 4.6.3 console build is not installed here): import (journal resolves post-rename); restoration suite 62/62; complete GUT suite 346/346; `git diff --check` clean. gdtoolkit not installed locally; CI remains the lint/format gate. | `scenes/restoration/restoration_view.tscn`, `scripts/restoration/restoration_view.gd`, `scripts/shop/shop_controller.gd`, `scenes/Book/Journal.tscn` (renamed), `scenes/Book/BookViewport.tscn`, `tests/restoration/test_bench_overlays.gd`, documentation updates |
| 2026-06-18 | Claude Code (Claude Opus 4.8) | Blemish ("decal") catalog + journal Blemish Guide page (Phase 9/13) | AI-assisted implementation: introduced "Blemish" as the in-game umbrella term for the soiling/accretions the player cleans off artifacts (each authored `SurfaceDecal.type` maps to a catalog entry). Added `BlemishType` model (`scripts/models/blemish_type.gd`: id, display_name, placeholder hex `color` swappable for a picture later, `cleaning_tool`, description); authored `data/journal/blemishes.json` (8 blemishes — dust, grime, rust, tarnish, water stain, fading, cracking, tape residue — each mapped to an existing tool); extended `DataRepository` to load a `journal` data directory, expose `get_blemish_type()`/`get_blemish_types_sorted()`, and cross-validate each blemish's cleaning tool. Extended the existing 3D journal: `Page.gd` now shows an `N / 5 fragments found` counter on the Fragment Case and renders a new **Blemish Guide** page (page 5) where each blemish is a note (colour swatch + name + cleaning tool); shifted the object-archive index to page 6 and entry pages to 7+, and updated `Book.gd` max-page math. Added `tests/journal/test_blemish_catalog.gd` (6 tests). Recorded the team's Day-5 visit-gate decision (an NPC appears on Day 5 only after completing beats 1 and 2; the Buyer is exempt) in `docs/phase-task.md` P10.2. Blemish swatch colours are placeholders pending authored pictures; the on-screen journal/Blemish Guide rendering is a pending manual gate. | Team must review the blemish names/colours/tool mappings, the journal page reordering, and the Day-5 gate wording. No historical claim, art, audio, dialogue, or runtime AI was generated; guide blurbs are development placeholder prose. Automated checks pass under Godot 4.6.stable on this machine (the documented 4.6.3 console build is not installed here): import; journal suite 28/28; complete GUT suite 312/312; `git diff --check` clean. gdtoolkit (gdformat/gdlint) is not installed locally and was not run; CI remains the lint/format gate and the new code follows the repository's tab/blank-line conventions. | `scripts/models/blemish_type.gd`, `data/journal/blemishes.json`, `scripts/core/data_repository.gd`, `scenes/Book/Page.gd`, `scenes/Book/Book.gd`, `tests/journal/test_blemish_catalog.gd`, `docs/phase-task.md`, this disclosure |
| 2026-06-18 | Claude Code (Claude Opus 4.8) | Cleaned-spot preservation now persists through save/reload (extends the prior in-memory fix) | AI-assisted follow-up: the dirt-mask preservation now survives a full save/reload, not just in-session switching. `ObjectInstance` gained a `dirt_mask` field (the exact grime mask as a small lossless **PNG**, serialized base64 via `Marshalls` — empty means rebuild from condition). `RestorationObject3D.snapshot_dirt()/restore_dirt()` were replaced with PNG variants (`snapshot_dirt_png`/`restore_dirt_png`); `RestorationService.persist_dirt_mask(uid, png)` writes the mask onto the instance and saves (the write stays in the service even though the mask lives in presentation). `RestorationView._cache_current_dirt()` now also persists on switch-away/close, and `load_instance` restores from the in-memory cache, else the persisted `dirt_mask`, else the condition reconstruction. PNG keeps the per-instance save cost small (a mostly-uniform 64×64 mask compresses to a few hundred bytes) and only condition-based objects store one (decal photos still use `removed_decals`). Added `test_cleaned_spots_persist_through_save_and_reload` (cleans, closes, reloads from disk into a fresh view with no in-memory cache, asserts coverage preserved). | Team must review the per-instance save-size impact for many in-progress restorations and confirm reloaded masks render correctly on-screen. No historical claim, art, audio, dialogue, or runtime AI was generated. Automated checks pass under Godot 4.6.stable (the documented 4.6.3 console build is not installed here): import; restoration suite 64/64; model round-trip + save tests green; complete GUT suite 348/348; `git diff --check` clean. gdtoolkit not installed locally; CI remains the lint/format gate. | `scripts/models/object_instance.gd`, `scripts/restoration/restoration_object_3d.gd`, `scripts/restoration/restoration_service.gd`, `scripts/restoration/restoration_view.gd`, `tests/restoration/test_restoration_view.gd`, documentation updates |
| 2026-06-18 | Claude Code (Claude Opus 4.8) | On-screen blemish cleaning in the 3D restoration bench (Phase 13, REST-R8) | AI-assisted implementation: extended the focused 3D restoration view to clean decal-based photos/frames by clicking discrete blemishes (not a shader dirt mask). `RestorationObject3D` gained a photo/blemish mode — a flat photo plane (placeholder QuadMesh; the team will swap in a scaled Book `Page`/PNG and a 3D frame model) carrying one hotspot per authored blemish, coloured from the blemish catalog, with analytic ray-sphere picking (`ray_test_blemish`), `remove_blemish`, auto-target, and visible-blemish queries. `RestorationView` detects decal-based templates, enters photo mode, routes a left-click (or controller auto-target) to `RestorationService.clean_decal` and removes the hit hotspot, shows blemish-progress + join/scan prompts, and performs the Archival Tape join (`join_object`) via click or the open/join action once clean. `RestorationService._can_restore_instance` now admits non-openable decal templates so photos reach the bench. Added `tests/restoration/test_photo_restoration_view.gd` (5 tests) driving cleaning/join through analytic world-space rays. Every gameplay rule still lives in `RestorationService` (the view never branches on carrier identity and never writes through `SaveService`); geometry/colours are placeholders. | Team must review the photo-plane/blemish placeholder geometry and placement, the click/controller interaction, and the join flow on-screen (mouse + controller/touch). No historical claim, art, audio, dialogue, or runtime AI was generated. Automated checks pass under Godot 4.6.stable on this machine (the documented 4.6.3 console build is not installed here): import; restoration suite 60/60; complete GUT suite 317/317; `git diff --check` clean. gdtoolkit not installed locally; CI remains the lint/format gate and the new code follows the repository's tab/blank-line conventions. On-screen mouse/controller/touch verification of the photo-cleaning and join is a pending manual gate. | `scripts/restoration/restoration_object_3d.gd`, `scripts/restoration/restoration_view.gd`, `scripts/restoration/restoration_service.gd`, `tests/restoration/test_photo_restoration_view.gd`, documentation updates |
| 2026-06-18 | Claude Code (Claude Opus 4.8) | Phone Marketplace interactable + Buy screen (Phase 10/14) | AI-assisted implementation: the existing shop Phone interactable (and its HUD fallback button) now opens a `MarketplaceScreen` (`scripts/economy/marketplace_screen.gd` + `scenes/ui/marketplace_screen.tscn`) instead of a placeholder dialogue. The screen is a CanvasLayer that builds its UI in code: money readout, a scrollable list of buyable tools (name, price, durability, ship time, per-row Buy button disabled when unaffordable), an incoming-shipments line, a "Sell — coming soon" placeholder, and Close; it acquires/releases a new `DayClock.PAUSE_PHONE` owner on open/close (defensively, like `BookViewport`) and closes on Esc. Buying delegates entirely to `MarketplaceService`. Wired into `ShopController` (`_on_phone_pressed` opens it; `_on_marketplace_closed` re-enables the shop props and refreshes the HUD). Updated the existing shop smoke test (the left-click dialogue check moved off the phone onto the door) and added `test_phone_opens_marketplace_and_pauses`; added `tests/economy/test_marketplace_screen.gd` (4 tests: pause acquire/release, pause composition, buying through the UI). Selling/banter remains deferred. | Team must review the Marketplace screen layout/readability on-screen (1920x1080 / 1280x720), mouse + controller/touch reachability of the phone prop and Buy buttons, and the shipment-pending copy. No historical claim, art, audio, dialogue, or runtime AI was generated. Automated checks pass under Godot 4.6.stable on this machine (the documented 4.6.3 console build is not installed here): import; economy suite 19/19; complete GUT suite 337/337; `git diff --check` clean. gdtoolkit not installed locally; CI remains the lint/format gate and the new code follows the repository's tab/blank-line conventions. On-screen verification of the phone → Marketplace flow is a pending manual gate. | `scripts/economy/marketplace_screen.gd`, `scenes/ui/marketplace_screen.tscn`, `scripts/shop/shop_controller.gd`, `scripts/core/day_clock.gd`, `tests/test_shop_smoke.gd`, `tests/economy/test_marketplace_screen.gd`, documentation updates |
| 2026-06-18 | Claude Code (Claude Opus 4.8) | Storage screen (Artifacts / Tools / Key Items) + bench loadout wiring (Phase 14) | AI-assisted implementation of the Storage UI that prepares the workbench, with three tabs as requested: **Artifacts** (ordinary `deliverable` restorable inventory — pick the one to restore next, setting `restore_target_uid`), **Tools** (owned tool instances with durability — toggle up to 10 into the bench loadout via `ToolService`), and **Key Items** (quest artifacts = non-deliverable inventory objects, plus the five fragments with their LOCKED/RELEASED/SEATED state and an `N/5 seated` line). `StorageScreen` (`scripts/economy/storage_screen.gd` + `scenes/ui/storage_screen.tscn`) is a code-built CanvasLayer using a `TabContainer`; it owns a new `DayClock.PAUSE_STORAGE`. A **Storage** button was added to `ShopHud` in code (no Shop.tscn edit) and wired through `ShopController` to open the screen (pausing time, disabling shop props). The bench now honours the prep: `RestorationView` lists tools from `RestorationService.get_workbench_tools()` (the ≤10 loadout, falling back to all owned when none is set) and pre-selects the chosen `restore_target_uid`. Added `tests/economy/test_storage_screen.gd` (6 tests: pause acquire/release, set restore target, load/unload bench, max-10 enforcement, three-tab render) and a shop smoke `test_storage_button_opens_storage_and_pauses`. | Team must review the three-tab split (esp. the "quest artifact = non-deliverable inventory" interpretation of Key Items), the on-screen layout/readability, and mouse + controller/touch reachability. No historical claim, art, audio, dialogue, or runtime AI was generated. Automated checks pass under Godot 4.6.stable on this machine (the documented 4.6.3 console build is not installed here): import; economy suite 25/25; complete GUT suite 344/344; `git diff --check` clean. gdtoolkit not installed locally; CI remains the lint/format gate and the new code follows the repository's tab/blank-line conventions. On-screen verification of the Storage flow is a pending manual gate. | `scripts/economy/storage_screen.gd`, `scenes/ui/storage_screen.tscn`, `scenes/ui/shop_hud.gd`, `scripts/shop/shop_controller.gd`, `scripts/restoration/restoration_view.gd`, `scripts/core/day_clock.gd`, `tests/economy/test_storage_screen.gd`, `tests/test_shop_smoke.gd`, documentation updates |
| 2026-06-18 | Claude Code (Claude Opus 4.8) | Tool durability + phone Marketplace (buy) + bench loadout — logic core (Phase 13/14) | AI-assisted implementation of the economy logic core (selling/banter deliberately deferred to the Phase-14 server-side negotiation). Tools become durability-tracked **instances**: new `ToolInstance` model; `ToolDefinition` gained `durability` (<=0 = infinite/never wears), `buyable`, and `ship_hours`; `LoopState` gained `owned_tools`, `workbench_tools` (uids), `tool_shipments`, and `restore_target_uid` (all loop-scoped). `ToolService` (`scripts/economy/tool_service.gd`) grants instances and manages the bench loadout (max 10) + restore target. `MarketplaceService` autoload (`scripts/economy/marketplace_service.gd`) lists buyable tools, spends `loop.money` on purchase, schedules a shipment that **arrives after `ship_hours` of in-game time** (delivered on the hour tick), and grants the instance on arrival. `RestorationService` now consumes one durability per use (most-worn instance first), removes a tool at 0 and drops it from the loadout (`EventBus.tool_broke`), and treats ownership/availability as id-set OR usable instance, with a new `get_workbench_tools()` loadout accessor. This is **additive**: tools owned via the existing id-set (starting kit / legacy rewards) never wear, so the SpawnDirector winnability checks and all prior tests are unchanged. New EventBus signals: `tool_broke`, `tool_purchased`, `tool_arrived`. Authored `durability`/`buyable`/`ship_hours` on the tools in `data/objects/tools.json`. Added `tests/economy/` (15 tests: durability/break/idset, marketplace buy/ship/arrival/insufficient-funds/non-buyable/multiples, loadout max-10/ownership/restore-target). The phone Marketplace & Storage **UI screens** and wiring the bench to show only the 10-tool loadout are the next step (not built here). | Team must review the durability values/ship times/prices, the most-worn-first wear rule, the loop-scoped vs persistent split for bought tools, and the additive dual-ownership model. No historical claim, art, audio, dialogue, or runtime AI was generated; no selling/LLM-banter was implemented. Automated checks pass under Godot 4.6.stable on this machine (the documented 4.6.3 console build is not installed here): import; economy suite 15/15; restoration 60/60; complete GUT suite 332/332; `git diff --check` clean. gdtoolkit not installed locally; CI remains the lint/format gate and the new code follows the repository's tab/blank-line conventions. | `scripts/models/tool_instance.gd`, `scripts/models/tool_definition.gd`, `scripts/models/save_state.gd`, `scripts/economy/tool_service.gd`, `scripts/economy/marketplace_service.gd`, `scripts/restoration/restoration_service.gd`, `scripts/core/event_bus.gd`, `project.godot`, `data/objects/tools.json`, `tests/economy/*`, documentation updates |
| 2026-06-20 | Jorge Maverick Acidre / team implementation | Compatibility decal shader and renderer fallback | Added scenes/restoration/compat_decal.gdshader and fallback logic in ArtifactConditionDecal so condition decals remain visible when the renderer is Compatibility. This supports the Mobile renderer as the default while keeping low-end/web viable. | Team must verify decal visuals match on Mobile vs Compatibility and that fallback detection does not misreport. No AI output was generated. | scenes/restoration/compat_decal.gdshader, scenes/restoration/artifact_condition_decal.tscn, scripts/restoration/artifact_condition_decal.gd |
| 2026-06-20 | Jorge Maverick Acidre / team implementation | SettingsService and global pause menu | Added persisted settings (resolution, fullscreen, Mobile/gl_compatibility renderer, online services toggle, artifact previews) via SettingsService (scripts/core/settings_service.gd) and a global pause overlay (scripts/ui/pause_menu.gd + scenes/ui/pause_menu.tscn) that pauses the tree, exposes settings, and returns to title/exit. | Team must review settings UI at 1920x1080 and 1280x720, renderer fallback detection, and the online-services toggle. No AI output was generated. | scripts/core/settings_service.gd, scripts/ui/pause_menu.gd, scenes/ui/pause_menu.tscn, tests/core/test_settings_service.gd, project.godot |
| 2026-06-21 | Jorge Maverick Acidre / team implementation | Backend local LLM provider for buyer banter | Extended the Node /api/negotiate proxy with an LLM_PROVIDER switch (anthropic \| local \| auto) so the backend can call a local OpenAI-compatible server (Ollama default http://localhost:11434/v1/chat/completions) instead of Anthropic. Same persona/guardrail prompt, offended/moderation contract, and deterministic offline fallback on failure. | Team must review the local-model prompt for false positives/negatives; model weights are the player-supplied local install and are not shipped. Backend secrets stay server-side. Server Jest 22/22 (incl. local-provider tests). | server/src/config.js, server/src/services/negotiate_service.js, server/.env.example, server/tests/negotiate.test.js, scripts/core/backend_launcher.gd |
| 2026-06-21 | Claude Opus 4.8 (Anthropic, via Claude Code) | Marketplace free-text banter + contextual moderation | Extended the existing backend `/api/negotiate` LLM proxy: the buyer now replies as structured JSON `{buyer_message, offended}` so the model both speaks in character (varied, non-canned) AND judges, in context, whether the seller's free-text message is offensive/NSFW/harassing/creepy (e.g. being hit on) — setting `offended:true` ends the deal in character. Added `parseReply()` (tolerant JSON/code-fence/plain-text parsing) and `negotiation_client.gd.fetch_chat()`. Offline/headless fallback remains the deterministic `Negotiation` engine plus a client-side keyword `ContentModeration` denylist; the LLM is never required and never decides prices. | Team must review the moderation prompt for false positives/negatives and confirm the guardrail (no price-setting, no instruction-following from seller input, never breaks character). Backend secrets stay server-side; rate limiting + sanitized input unchanged. Automated checks pass: full GUT suite green; server Jest 20/20 (incl. new `parseReply`/offended tests). Live-model behavior (actual LLM replies + contextual offense detection) requires manual verification with a real `ANTHROPIC_API_KEY`. | `server/src/services/negotiate_service.js`, `scripts/economy/negotiation_client.gd`, `scripts/economy/negotiation.gd`, `scripts/economy/content_moderation.gd`, `scripts/economy/phone.gd`, `server/tests/negotiate.test.js` |
| 2026-06-22 | Jorge Maverick Acidre / team implementation | Buyer personas, wallets, and deterministic negotiation engine | Authored nine buyer personas in data/buyers/buyers.json (collector, reseller, student, gift, hobbyist, appraiser, tourist, lola, suspicious/Mr. Maverick) with budgets, category prefs, negotiation styles, and fallback line sets. Added BuyerPersona model, per-loop wallets, persistent same-day haggle sessions, and the deterministic Negotiation engine (scripts/economy/negotiation.gd) that factors condition, category, honesty, and banter mood. | Team must review persona tuning, price curves, and Maverick ghosting rules. No AI-generated output for the core engine; lines are authored or deterministic. | data/buyers/buyers.json, scripts/models/buyer_persona.gd, scripts/economy/marketplace_service.gd, scripts/economy/negotiation.gd, tests/economy/test_buyer_personas.gd, tests/economy/test_negotiation.gd, tests/economy/test_marketplace.gd |
| 2026-06-22 | Kimi Code CLI (Moonshot AI) | Documentation recovery pass | Updated docs/phase-task.md (Phase 13/14 status, verification commands, P0 requirement coverage), docs/PROMPT_CONTEXT.md (renderer, autoloads, test counts, playable state, implemented/missing systems), and docs/ai-disclosure.md (repaired malformed table, added local AI, settings/pause, renderer entries). No gameplay code, assets, or runtime AI output was generated. | Team must review the documentation for accuracy against the actual code and tests. All counts are from the 2026-06-22 GUT/server/mock-portal test runs. | docs/phase-task.md, docs/PROMPT_CONTEXT.md, docs/ai-disclosure.md |
| 2026-06-22 | Jorge Maverick Acidre / team implementation | On-device local LLM for buyer banter via NobodyWho | Added LocalAI autoload (scripts/economy/local_ai.gd) that optionally runs a local GGUF model through the NobodyWho addon (addons/nobodywho/). Uses ClassDB reflection so the project builds without the addon; when a model is present at res://models/model.gguf, the buyer replies and haggle decisions are generated on-device with no backend and no API key. Falls back to backend /api/negotiate or deterministic Negotiation on any failure. | This is runtime AI visible to players. Team must review the on-device guardrail prompt, output parsing, and moderation fallback. Model weights are not shipped; the player supplies their own. No API keys or provider URLs are committed. | scripts/economy/local_ai.gd, addons/nobodywho/, project.godot |
| 2026-06-22 | chuchuu / team implementation | Renderer switch to Mobile/Vulkan with Compatibility fallback | Changed project.godot to use the Mobile renderer by default (renderer/rendering_method=mobile, config features 4.6, Mobile) so Decal-based condition decals render correctly. Compatibility (gl_compatibility) remains the fallback for low-end/mobile/web; SettingsService.effective_renderer() detects the actual runtime renderer and locks Mobile if the device fell back. | Team must verify Mobile renderer stability on the Windows reference system and Compatibility parity on the web reference system. No AI output was generated. | project.godot, scripts/core/settings_service.gd |
| 2026-06-23 | Claude Code (Claude Opus 4.8) | Phase 10 Auntie showcase + vertical-slice integration | AI-assisted implementation connecting Auntie's route to the real loop without bypassing P0 systems. Added the `FragmentService` autoload (`scripts/core/fragment_service.gd`) owning the `LOCKED -> RELEASED` transition (persistent authority + repo mirror + atomic save + `EventBus.fragment_released`); changed `data/artifacts/fragments.json` so `fragment_01` starts LOCKED (no pre-release shortcut). Extended `RouteService` with authored-beat ordinal gating (`complete_beat`/`due_beat`/`is_beat_complete`), the Day-5 / beat visit gate, unanswered-visit consumption (`_on_hour_changed` -> `EventBus.visit_missed`), and a debug visit override; completing Auntie's final beat releases her fragment (never a handoff — the Spawn Director places it). Added a paused 2D `ShowcaseScreen` (`scripts/ui/showcase_screen.gd`) opened after Auntie's door dialogue and a DEBUG-only `DemoMenu` (`scripts/ui/demo_menu.gd`, F9 in debug builds: seed, 3-seed placement demo, debug release, two-press save-clear). Added `route_beats_completed` to `SaveState`, `route_beat_completed`/`fragment_released`/`visit_missed` signals, `PAUSE_SHOWCASE`/`PAUSE_DEMO`, and wired `ShopController`. Updated the Phase 5/8 tests that relied on `fragment_01` being released-by-default to release it explicitly (intent preserved); added `tests/core/test_fragment_service.gd`, `tests/core/test_route_beats.gd`, `tests/ui/test_showcase_screen.gd`, `tests/ui/test_demo_menu.gd`. | Team must review the LOCKED-default migration and its test updates, the visit-consumption semantics, the Day-5 gate, and the showcase prose/placeholder visuals (subject to cultural/native-speaker review — no new historical claims were authored; the showcase shows the existing data-authored beat summary plus generic supportive captions). No runtime/generative AI, art, audio, voice, or historical claim was produced. Automated checks pass under Godot 4.6.3: import exit 0; full GUT suite 474/474 (1531 asserts); focused core+ui 89/89; gdformat applied to new files; gdlint clean on new files (the one remaining `class-definitions-order` warning at `shop_controller.gd:46` is the pre-existing `ALYA_PORTRAIT` const). The uninterrupted on-screen end-to-end slice playthrough remains a pending manual gate. | `scripts/core/fragment_service.gd`, `scripts/core/route_service.gd`, `scripts/core/event_bus.gd`, `scripts/core/day_clock.gd`, `scripts/core/loop_controller.gd`, `scripts/models/save_state.gd`, `scripts/shop/shop_controller.gd`, `scripts/ui/showcase_screen.gd`, `scripts/ui/demo_menu.gd`, `data/artifacts/fragments.json`, `project.godot`, `tests/core/test_fragment_service.gd`, `tests/core/test_route_beats.gd`, `tests/ui/test_showcase_screen.gd`, `tests/ui/test_demo_menu.gd`, updated Phase 3/5/6 tests, documentation updates |

| 2026-06-23 | Kimi Code CLI (Moonshot AI) | Phase 11 export pipeline + QA docs | AI-assisted fix for the HTML5/Web export failure: identified that Godot 4.7's Web export validation silently requires `rendering/textures/vram_compression/import_etc2_astc=true`; enabled it and added `renderer/rendering_method.web="gl_compatibility"` so Windows stays on Mobile while Web uses Compatibility. Updated `export_presets.cfg` to exclude `addons/nobodywho/*` and `models/*` from the Web preset; verified Windows Desktop and Web CLI exports exit 0. Updated `docs/phase-task.md`, `docs/PROMPT_CONTEXT.md`, `CLAUDE.md`, and this disclosure; created human-run evidence runbooks under `docs/evidence/` for P11.3–P11.8. No runtime/generative AI, art, audio, voice, or historical claim was produced. | Team must run the Web build in a browser to confirm it loads and performs acceptably, must execute the manual runbooks and attach evidence, and must review the export settings and documentation for accuracy. The NobodyWho warning on Web export is expected and non-fatal. | `project.godot`, `export_presets.cfg`, `docs/phase-task.md`, `docs/PROMPT_CONTEXT.md`, `CLAUDE.md`, `docs/ai-disclosure.md`, `docs/evidence/p11_*_runbook.md` |

## Implemented Runtime AI

These systems are live in the current build and may produce player-visible output:

| System | Implementation | Required safeguards |
|---|---|---|
| On-device buyer banter | LocalAI autoload using NobodyWho GGUF (scripts/economy/local_ai.gd) | Addon/model are optional; falls back to backend or deterministic offline engine. Guardrail prompt prohibits price-setting, instruction-following, and breaking character; offended/moderation fallback exists. |
| Backend buyer banter | Node /api/negotiate proxy with Anthropic, local OpenAI-compatible (Ollama), or auto selection (server/src/services/negotiate_service.js) | Secrets/server-side only; rate limiting; sanitized input; persona/guardrail prompts; deterministic offline fallback. |
| Object scanner | Cached offline fixture responses (data/scanner-cache/scanner_cache.json); live backend /api/scan planned | Player always chooses final verdict; historical claims marked unverified pending review. |

## Planned Runtime AI

These systems are planned but are not implemented merely because they appear here:

| System | Planned role | Required safeguards |
|---|---|---|
| Cultural Echo source generation | Help produce original regional soundscape source material | Human curation, native-speaker review, no sampling or imitation of protected/traditional recordings |

## Review Gate

Before every milestone submission:

- [ ] Review this log against commits, assets, prompts, runtime dependencies, and generated content.
- [ ] Add missing historical-source and native-speaker review notes.
- [ ] Confirm no secrets, copyrighted source material, or undisclosed AI output ships.
- [ ] Include the final disclosure in the submitted repository and milestone package.